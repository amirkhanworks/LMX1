# Luminox Automation — Static Site (v11)

Plain HTML/CSS/JS. Files sit flat at the repo root.

## Files
- `index.html`, `about.html`, `services.html`, `styles.css`, `script.js`
- `fahad.png`, `saad.png`, `amir.png`
- `logo-wordmark.svg`, `logo-icon.svg`, `logo-lockup.svg` (unused)

## Resolved this round
- Switched from Formspree to Typeform for the main "Request a Demo" flow. No embed — every "Request a Demo" button/link across Home and About now opens `https://form.typeform.com/to/ywDES3IY` directly in a new tab (nav, hero, mid-page card links, final CTA). The old multi-field form is gone; the final CTA section is now just the Typeform button + the WhatsApp button, side by side.
- Testimonials finalized as-is, per your go-ahead.
- Stock photo confirmed, no change.

## Still open
1. **The "Be First to Know" email waitlist** (Home page, app launch signup) still points at an unconfigured Formspree placeholder — this wasn't part of the Typeform switch. Want it on the same Typeform, a second one, Formspree after all, or something else?
2. Real product/lifestyle photos, whenever ready
3. Your phone/device review — let me know what you find
